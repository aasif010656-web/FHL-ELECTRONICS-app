{
  "name": "fhl-electronics-mobile-recovery",
  "version": "5.9.5-recovery",
  "private": true,
  "description": "Capacitor Android rebuild starter generated from the supplied FHL Electronics APK.",
  "scripts": {
    "copy:web": "node scripts/copy-web.mjs",
    "android:sync": "npm run copy:web && npx cap sync android",
    "android:open": "npx cap open android",
    "android:build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/camera": "^6.0.1",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
